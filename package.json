{
  "name": "context-doctor-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test 'tests/**/*.test.ts'"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
